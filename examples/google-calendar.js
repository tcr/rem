// Generated by CoffeeScript 1.3.1
(function() {
  var fs, gcal, keys, read, rem;

  rem = require('../rem');

  fs = require('fs');

  read = require('read');

  keys = JSON.parse(fs.readFileSync(__dirname + '/keys.json'));

  gcal = rem.load('google-calendar', '3', {
    key: keys.google.key,
    secret: keys.google.secret
  });

  rem.oauthConsole(gcal, function(err, user) {
    return user('users/me/calendarList').get(function(err, json) {
      var cal, _i, _len, _ref, _results;
      if (err) {
        console.log(err);
        return;
      }
      console.log('Your calendars:');
      _ref = json.items;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        cal = _ref[_i];
        _results.push(console.log(' -', cal.summary));
      }
      return _results;
    });
  });

}).call(this);
